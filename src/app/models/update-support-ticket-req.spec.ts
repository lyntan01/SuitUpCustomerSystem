import { UpdateSupportTicketReq } from './update-support-ticket-req';

describe('UpdateSupportTicketReq', () => {
  it('should create an instance', () => {
    expect(new UpdateSupportTicketReq()).toBeTruthy();
  });
});
