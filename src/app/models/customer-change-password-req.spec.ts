import { CustomerChangePasswordReq } from './customer-change-password-req';

describe('CustomerChangePasswordReq', () => {
  it('should create an instance', () => {
    expect(new CustomerChangePasswordReq()).toBeTruthy();
  });
});
