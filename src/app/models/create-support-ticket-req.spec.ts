import { CreateSupportTicketReq } from './create-support-ticket-req';

describe('CreateSupportTicketReq', () => {
  it('should create an instance', () => {
    expect(new CreateSupportTicketReq()).toBeTruthy();
  });
});
