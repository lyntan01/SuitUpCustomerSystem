import { PercentagePromotion } from './percentage-promotion';

describe('PercentagePromotion', () => {
  it('should create an instance', () => {
    expect(new PercentagePromotion()).toBeTruthy();
  });
});
